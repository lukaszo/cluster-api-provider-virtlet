apiVersion: virtlet.cluster.k8s.io/v1alpha1
kind: VirtletClusterProviderSpec
metadata:
  labels:
    controller-tools.k8s.io: "1.0"
  name: virtletclusterproviderspec-sample
spec:
  # Add fields here
  foo: bar
